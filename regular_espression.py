import re

phrase_temlpate = 'like0'

if 'like' in phrase_temlpate:
    print('jdns')

# word_to_change_like = re.findall(r'like', phrase_temlpate)
# if len(word_to_change_like) > 0:
#     print('есть что менять')
#     print(word_to_change_like)